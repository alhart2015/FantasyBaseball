# data/analysis/ holds both committed deliverables and scratch script output.
# Default to ignoring, and allow the #266 study's artifacts explicitly. They are
# retained as the written record behind docs/superpowers/keeper-calibration-
# finding-2026-07-27.md; the script that produced them and the test that bound
# the shipped coefficients to them were removed with the fold, so nothing
# regenerates or checks these now.
*
!.gitignore
!keeper_calibration_*.csv
